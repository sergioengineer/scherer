import { Container, MenuItem } from "./styles"

const Menu: React.FC = () => {
  return (
    <Container>
      <MenuItem>Chamados</MenuItem>
      <MenuItem>Configurações</MenuItem>
    </Container>
  )
}

export default Menu
